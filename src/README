This has some little test programs for messing around with physics engines.

Run 'make' to build them.

Requires libBullet with development files.  Recommended that you always have
the latest version (2.81 as of this writing) because instabilities tend to get
fixed.

